var abs = Math.abs;

// Now you can use `abs` instead of `Math.abs`:
var number = -10;
var absoluteValue = abs(number); // equivalent to Math.abs(number)
console.log(absoluteValue); // Output: 10
